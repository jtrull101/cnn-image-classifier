{
  "name": "training",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "packages/training/img_classifier_training",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "uv build",
        "cwd": "packages/training"
      },
      "dependsOn": ["^build"]
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "uv run python -m pytest tests/test_training.py -v",
        "cwd": "."
      },
      "dependsOn": ["build"]
    },
    "test-cov": {
      "executor": "nx:run-commands",
      "options": {
        "command": "uv run python -m pytest tests/test_training.py tests/test_training_pytest.py -v --cov=packages/training --cov-report=term-missing",
        "cwd": "."
      },
      "dependsOn": ["build"]
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "uv run ruff check .",
        "cwd": "packages/training"
      }
    },
    "format": {
      "executor": "nx:run-commands",
      "options": {
        "command": "uv run ruff format .",
        "cwd": "packages/training"
      }
    }
  },
  "tags": ["type:package", "scope:training"],
  "implicitDependencies": ["config", "data", "models"]
}

