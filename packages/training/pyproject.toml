[project]
name = "alz-mri-training"
version = "0.1.0"
description = "Training pipeline for Alzheimer's MRI CNN"
authors = [{ name = "Jonathan Trull" }]
readme = "README.md"
requires-python = ">=3.11,<3.14"
license = { text = "MIT" }

dependencies = [
    "alz-mri-config",
    "alz-mri-data",
    "alz-mri-models",
    "tensorflow>=2.13.0",
    "keras>=2.13.0",
    "matplotlib>=3.7.0",
    "pandas>=2.0.0",
    "seaborn>=0.12.0",
]

[tool.uv.sources]
alz-mri-config = { workspace = true }
alz-mri-data = { workspace = true }
alz-mri-models = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["alz_mri_training"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py313"

[tool.pyright]
typeCheckingMode = "basic"
pythonVersion = "3.13"
